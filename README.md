# SoftwareQA-Week7

This repo contains the `sections` and `evidence` directories as required by the Python Unit Testing assignment.

To run the section 2 test for example:
- `cd ./unit-testing-assignment/sections`
- `python3 -m unittest -v section2.tests`

To run all tests in repo:
- `python3 -m unittest -v`
